<form method="post" action="{{ path('prestation_gauge_delete', {'id': prestation_gauge.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cet élément?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ prestation_gauge.id) }}">
    <button class="btn">Supprimer</button>
</form>
