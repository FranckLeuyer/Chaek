<form method="post" action="{{ path('prestation_object_delete', {'id': prestation_object.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ prestation_object.id) }}">
    <button class="btn">Delete</button>
</form>
