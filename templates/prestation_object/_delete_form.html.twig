<form method="post" action="{{ path('prestation_object_delete', {'id': prestation_object.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cet élément?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ prestation_object.id) }}">
    <button class="btn">Supprimer</button>
</form>
