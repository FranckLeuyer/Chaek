{% extends 'base.html.twig' %}

{% block title %}Objet de prestations{% endblock %}

{% block body %}
    <h1>Objet de prestations</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ prestation_object.id }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ prestation_object.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('prestation_object_index') }}">retour à la liste</a>

    <a href="{{ path('prestation_object_edit', {'id': prestation_object.id}) }}">Modifier</a>

    {{ include('prestation_object/_delete_form.html.twig') }}
{% endblock %}
